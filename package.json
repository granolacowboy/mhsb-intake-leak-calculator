{
  "name": "mhsb-intake-leak-calculator",
  "type": "module",
  "version": "1.0.0",
  "private": true,
  "description": "Standalone static tool: estimate a law firm's annual intake revenue leak. Zero backend, client-side only.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:e2e": "playwright test",
    "lint:brand": "bash scripts/brand-lint.sh",
    "verify": "npm run test:unit && npm run build && npm run test:e2e && npm run lint:brand"
  },
  "dependencies": {
    "@fontsource/ibm-plex-serif": "^5.1.0",
    "@fontsource/inter": "^5.1.0",
    "astro": "^7.3.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.6",
    "@axe-core/playwright": "^4.10.2",
    "@playwright/test": "^1.63.0",
    "@types/node": "^26.6.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
